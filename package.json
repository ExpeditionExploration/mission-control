{
    "name": "mission-control",
    "version": "0.0.1",
    "scripts": {
        "build": "gulp build",
        "deploy": "npm run build && gulp deploy",
        "update": "npm run build && gulp deploy --update",
        "install:server": "cd server && npm install",
        "install:client": "cd client && npm install",
        "postinstall": "npm run install:server && npm run install:client"
    },
    "dependencies": {
        "gulp": "^4.0.2",
        "minimist": "^1.2.7",
        "yargs": "^17.6.2"
    }
}
